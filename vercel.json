{
  "framework": "nextjs",
  "buildCommand": "rm -rf apps/web/.next/cache && pnpm core:build && pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "devCommand": "pnpm --filter web dev"
}
